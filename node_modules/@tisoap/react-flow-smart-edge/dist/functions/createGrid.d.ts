import { Grid } from 'pathfinding';
import type { NodeBoundingBox, GraphBoundingBox } from './getBoundingBoxes';
import type { Position } from 'react-flow-renderer';
export declare type PointInfo = {
    x: number;
    y: number;
    position: Position;
};
export declare const createGrid: (graph: GraphBoundingBox, nodes: NodeBoundingBox[], source: PointInfo, target: PointInfo, gridRatio?: number) => {
    grid: Grid;
    start: import("react-flow-renderer").XYPosition;
    end: import("react-flow-renderer").XYPosition;
};
